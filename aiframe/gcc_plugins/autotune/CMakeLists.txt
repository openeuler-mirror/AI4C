file(GLOB_RECURSE SOURCE_FILES "*_plugin.cpp")
foreach(SOURCE_FILE ${SOURCE_FILES})
  get_filename_component(TARGET_NAME ${SOURCE_FILE} NAME_WE)
  add_gcc_plugin(${TARGET_NAME} ${SOURCE_FILE})
endforeach()